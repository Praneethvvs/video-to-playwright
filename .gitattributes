# Normalise to LF in the repository so clones on macOS and Linux get sane files,
# while Windows checkouts still get CRLF locally.
* text=auto eol=lf

# Binary — never touch these.
*.png binary
*.jpg binary
*.mp4 binary
*.webm binary
*.docx binary
*.zip binary
